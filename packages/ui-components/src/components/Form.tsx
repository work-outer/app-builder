
import React from "react";

// https://developer.salesforce.com/docs/component-library/bundle/lightning-record-form/documentation
export class Form extends React.Component<any> {
  static defaultProps = {
  }

  render() {
    const {fields, initialValues, children, ...rest} = this.props

    return (
      <>
         
      </>
    )
  }
}
